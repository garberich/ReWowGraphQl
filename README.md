# Bienvenido. Backend desarrollado en GraphQl para aplicación ReWow

## Instrucciones

- Ejecutar `npm install` para instalar las dependencias
- Ejecutar con `npm start`
- Abrir el servidor el Playground en `http://localhost:4000/`